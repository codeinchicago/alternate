-- Can set sql_mode to ANSI to truncate overly long strings, those longer than char(xx)
-- Rather than throwing an error.

-- Can search within a string, SELECT POSITION('characters' in vchard_fld)
-- FROM string_tbl; (p.122 in book)

